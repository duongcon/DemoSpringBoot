server.port=9999
#server.address=localhost:9999/api/v1
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://10.0.65.25:5432/mbal_backend_production
spring.datasource.username=mbal_backend
spring.datasource.password=Dinosys@2019

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

# Hibernate ddl auto (create, create-drop, validation, update)
spring.jpa.hibernate.ddl-auto = update

# Fix Postgres JPA Error:
# Method org.postgresql.jdbc.PgConnection.createClob() is not yet implemented.
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
###
#spring.security.user.name=duke
#spring.security.user.password=duke

#Set time logout
